# Where objathor retriever data lives. Leave unset to use the default
# `uv run python scripts/download_data.py` fetches to data/objathor-assets by default
# Set this elsewhere only if you'd like to keep the data in a different location.
export OBJATHOR_ASSETS_BASE_DIR=data/objathor-assets
export OPENAI_API_KEY=sk-your-openai-api-key
