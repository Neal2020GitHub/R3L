# Issue

You made ${a} mistake in the constraint program.
Please read carefully the error message and fix the issue accordingly. 
Afterwards, output a **complete** program (do not omit any parts of the code).
Adhere strictly to aforementioned output format.

REMINDER: ALL numeric parameters (clearance, percentile, distance, sweep_deg, angle) 
MUST be Var references, not raw numeric literals.

```
${e}
```